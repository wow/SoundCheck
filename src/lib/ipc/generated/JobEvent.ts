// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { IpcError } from "./IpcError";
import type { Plan } from "./Plan";
import type { RowAnalysis } from "./RowAnalysis";

/**
 * What a batch job reports to the UI, in order: per file `started`, `progress` at most every
 * 100 ms, then exactly one of `analysed`, `failed` or `cancelled`; a `batch` summary at most
 * every 500 ms; `finished` last. A job that cannot start sends `aborted` and then `finished`.
 */
export type JobEvent = { "type": "started", 
/**
 * The job.
 */
jobId: number, 
/**
 * The file.
 */
fileId: number, } | { "type": "progress", 
/**
 * The job.
 */
jobId: number, 
/**
 * The file.
 */
fileId: number, 
/**
 * Fraction done.
 */
fraction: number, } | { "type": "analysed", 
/**
 * The job.
 */
jobId: number, 
/**
 * The file.
 */
fileId: number, 
/**
 * The table's fields (boxed: much larger than every other event).
 */
row: RowAnalysis, 
/**
 * What processing would do.
 */
plan: Plan, 
/**
 * The settings revision `plan` was decided with; a newer replan wins.
 */
revision: number, } | { "type": "failed", 
/**
 * The job.
 */
jobId: number, 
/**
 * The file.
 */
fileId: number, 
/**
 * Why.
 */
error: IpcError, } | { "type": "cancelled", 
/**
 * The job.
 */
jobId: number, 
/**
 * The file.
 */
fileId: number, } | { "type": "batch", 
/**
 * The job.
 */
jobId: number, 
/**
 * Files with a final event.
 */
done: number, 
/**
 * Files in the job.
 */
total: number, 
/**
 * Remaining time, ms, once a file has finished.
 */
etaMs: number | null, 
/**
 * Seconds of audio per second of wall time so far.
 */
realtimeX: number | null, } | { "type": "aborted", 
/**
 * The job.
 */
jobId: number, 
/**
 * Why.
 */
error: IpcError, } | { "type": "finished", 
/**
 * The job.
 */
jobId: number, 
/**
 * Whether it was cancelled.
 */
cancelled: boolean, };
