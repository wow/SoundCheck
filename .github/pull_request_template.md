## Summary

<!-- One paragraph: what changed and why. The PR title is the squash-commit subject: type(scope): imperative subject -->

## User-visible behaviour

<!-- What a user sees differently, with numbers (dB, LU, ms) where relevant. "None" is fine. -->

## Tests run

<!-- Paste the `./scripts/verify.sh` summary. Name new tests and their tolerances. -->

## Output change

- [ ] No: the same input still yields the same numbers, grids and bytes.
- [ ] Yes: goldens regenerated and reviewed; the commit carries `Output-Change: yes`; this ships in a minor release.

## Docs and dependencies

- [ ] `docs/PRODUCT.md` / `docs/ARCHITECTURE.md` updated if behaviour or crate boundaries changed.
- [ ] New dependencies, with licence: <!-- crate or package: licence -->
