# sc-cli

The headless SoundCheck binary. `sc-cli analyze <file> --json` prints exactly the fields the desktop table shows; `--version` prints the version with the git revision and build date. Later: `process`, `grid-check`, `bench`, `undo`, `eval`.
